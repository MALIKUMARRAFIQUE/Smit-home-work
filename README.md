# Customer Churn Predictor

## Run locally

```powershell
python -m pip install -r requirements.txt
python -m streamlit run streamlit_app.py
```

Open `http://localhost:8501`, enter the customer details, and click **Check churn risk**.
